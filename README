Byron Becker

Lab 4 - Version Control

1. Create a public git repository
	created a rsa key and linked it to my github accnt using 
	https://help.github.com/articles/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent/
	downloaded homebrew
	linked hub to git
	configd my git username and email
2. Add and commit a simple README and a few other file
	git add . 
	git commit -m "Commit message here"
3. Create a tag named v0.01
	git tag v0.01
4. Push all changes and tags to your remote repo4. 
	git push origin tag v0.01
5. Create and checkout a new branch named testing-new-files
	git checkout -b testing-new-files
6. Add and commit a few more files to this branch
	git add .
	git commit -m "Updated Readme and added new files"
	git push origin testing-new-files
7. Checkout the master branch
	git checkout master
8. Edit and commit at least one of the files in this branch
